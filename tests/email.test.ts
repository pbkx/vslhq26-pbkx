import{describe,expect,it}from"vitest";import{AzureCommunicationEmailService}from"../src/services/emailService.js";describe("ACS Email",()=>{it("returns preview-only when credentials are placeholders",async()=>{const result=await new AzureCommunicationEmailService().send({to:"demo@example.org",subject:"GrantPilot",plainText:"Test",html:"<p>Test</p>"});expect(result).toEqual({provider:"azure-communication-services",status:"preview-only"})})});
