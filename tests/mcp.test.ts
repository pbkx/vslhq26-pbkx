import{afterEach,describe,expect,it}from"vitest";import{Client}from"@modelcontextprotocol/sdk/client/index.js";import{InMemoryTransport}from"@modelcontextprotocol/sdk/inMemory.js";import{createGrantPilotMcpServer,REQUIRED_TOOLS}from"../src/mcp/createServer.js";
let close:(()=>Promise<void>)|undefined;afterEach(()=>close?.());describe("GrantPilot MCP",()=>{it("discovers tools, returns structured grants, enforces count limits, paginates cached results, and serves the workbench",async()=>{const[a,b]=InMemoryTransport.createLinkedPair(),server=createGrantPilotMcpServer(),client=new Client({name:"test",version:"1"});await server.connect(b);await client.connect(a);close=async()=>{await client.close();await server.close()};const tools=await client.listTools();expect(tools.tools.map(x=>x.name).sort()).toEqual([...REQUIRED_TOOLS].sort());const search=tools.tools.find(x=>x.name==="search_grants")!;expect((search._meta as any).ui.resourceUri).toBe("ui://grantpilot/opportunity-workbench-v3");expect((search.inputSchema as any).properties.resultTypes.items.enum).toEqual(["current-federal","forecasted-federal","historical-private-prospect"]);expect((search.inputSchema as any).properties.requestedResultCount).toBeTruthy();expect(search.description).toContain("never supplement");const limited=await client.callTool({name:"search_grants",arguments:{query:"AI workforce",requestedResultCount:7}}),limitedOutput=limited.structuredContent as any;expect(limitedOutput.totalResultCount).toBeLessThanOrEqual(7);const rejected=await client.callTool({name:"search_grants",arguments:{query:"AI workforce",requestedResultCount:101}});expect(rejected.isError).toBe(true);expect((rejected.content as any)[0].text).toContain("between 1 and 100");expect((rejected.structuredContent as any).error.code).toBe("RESULT_LIMIT_EXCEEDED");const result=await client.callTool({name:"search_grants",arguments:{query:"AI workforce"}}),output=result.structuredContent as any;expect(output.grants.length).toBeGreaterThan(5);expect(output.context.projectTitle).toBeTruthy();expect((result.content as any)[0].text).toContain("Do not add or recommend any outside funder");expect(Buffer.byteLength(JSON.stringify(result))).toBeLessThan(48*1024);if(output.hasMore){const more=await client.callTool({name:"load_more_grants",arguments:{queryId:output.queryId,offset:output.nextOffset}}),page=more.structuredContent as any;expect(page.append).toBe(true);expect(page.grants.length).toBeGreaterThan(0);expect(page.grants.some((grant:any)=>output.grants.some((first:any)=>first.opportunity.id===grant.opportunity.id))).toBe(false);expect(Buffer.byteLength(JSON.stringify(more))).toBeLessThan(48*1024)}const resource=await client.readResource({uri:"ui://grantpilot/opportunity-workbench-v3"});expect(resource.contents[0].mimeType).toBe("text/html;profile=mcp-app");expect((resource.contents[0]as any).text).toContain("GrantPilot")})});
