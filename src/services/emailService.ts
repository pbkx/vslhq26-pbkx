import{EmailClient}from"@azure/communication-email";
export type EmailMessage={to:string;subject:string;plainText:string;html:string};export type EmailDelivery={provider:"azure-communication-services";status:"sent"|"preview-only";messageId?:string};export interface EmailService{send(message:EmailMessage):Promise<EmailDelivery>}
export class AzureCommunicationEmailService implements EmailService{async send(message:EmailMessage):Promise<EmailDelivery>{const connection=process.env.AZURE_COMMUNICATION_SERVICES_CONNECTION_STRING,sender=process.env.EMAIL_SENDER_ADDRESS;if(!connection||!sender)return{provider:"azure-communication-services",status:"preview-only"};const client=new EmailClient(connection),poller=await client.beginSend({senderAddress:sender,content:{subject:message.subject,plainText:message.plainText,html:message.html},recipients:{to:[{address:message.to}]}}),result=await poller.pollUntilDone();return{provider:"azure-communication-services",status:"sent",messageId:result.id}}}
export const emailService:EmailService=new AzureCommunicationEmailService();
