import{mkdir,readFile,rename,writeFile}from"node:fs/promises";import{resolve}from"node:path";import type{GrantResult,GrantWatch,SearchOutput}from"../domain/types.js";
class GrantRepository{private searches=new Map<string,SearchOutput>();private grants=new Map<string,GrantResult>();private watches=new Map<string,GrantWatch>();private path=resolve(process.env.GRANTPILOT_STORE_PATH??"cache/grants/state.json");async hydrate(){try{const data=JSON.parse(await readFile(this.path,"utf8"));for(const q of data.searches??[])this.saveSearch(q);for(const w of data.watches??[])this.watches.set(w.id,w)}catch{}}saveSearch(search:SearchOutput){this.searches.set(search.queryId,search);search.grants.forEach(g=>this.grants.set(g.opportunity.id,g));return search}getSearch(id:string){const value=this.searches.get(id);if(!value)throw new Error(`Unknown or expired query: ${id}`);return value}getGrant(id:string){const value=this.grants.get(id);if(!value)throw new Error(`Unknown grant: ${id}`);return value}async saveWatch(watch:GrantWatch){this.watches.set(watch.id,watch);await this.persist();return watch}listWatches(){return[...this.watches.values()]}async deleteWatch(id:string){const removed=this.watches.delete(id);await this.persist();return removed}private async persist(){await mkdir(resolve(this.path,".."),{recursive:true});const temp=`${this.path}.${process.pid}.tmp`;await writeFile(temp,JSON.stringify({searches:[...this.searches.values()].slice(-10),watches:this.listWatches()},null,2));await rename(temp,this.path)}clear(){this.searches.clear();this.grants.clear();this.watches.clear()}}
export const grantRepository=new GrantRepository();await grantRepository.hydrate();
